/// <reference path="api.d.ts" />
module {{module}} {
    'use strict';

    export class {{className}} {{#ancestorClass}}extends {{.}}{{/ancestorClass}} {

    {{#properties}}
        {{#description}}/*{{.}}*/{{/description}}
        {{name}}: {{#type.name}}{{.}}{{/type.name}}{{#unless type.name}} {
            {{#type.properties}}
                {{name}}: {{type.name}};
            {{/type.properties}}
        } {{/unless}};

    {{/properties}}
    }
}
